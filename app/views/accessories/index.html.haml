.container
  %h3 電子アクセサリー一覧
  %span みんなが作ったアクセサリーをみてみよう！

  %table
    %thead
      %tr
        %th Name
        %th Age
        %th
        %th
        %th

    %tbody
      - @accessories.each do |hoge|
        %tr
          %td= hoge.name
          %td= hoge.age
          %td= link_to 'Show', hoge
          %td= link_to 'Edit', edit_accessory_path(hoge)
          %td= link_to 'Destroy', hoge, method: :delete, data: { confirm: 'Are you sure?' }
